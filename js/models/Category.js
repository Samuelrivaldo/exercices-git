/**\n * Category Model - Représente une catégorie de tâches\n */\nclass Category {\n    constructor({ id = null, name = '', color = '#3498db', icon = '📁' } = {}) {\n        this.id = id || `cat_${Date.now()}`;\n        this.name = name;\n        this.color = color;\n        this.icon = icon;\n    }\n\n    toJSON() {\n        return {\n            id: this.id,\n            name: this.name,\n            color: this.color,\n            icon: this.icon\n        };\n    }\n}\n\n/**\n * Tag Model - Représente un tag/label\n */\nclass Tag {\n    constructor({ id = null, name = '', bgColor = '#ecf0f1', textColor = '#333' } = {}) {\n        this.id = id || `tag_${Date.now()}`;\n        this.name = name;\n        this.bgColor = bgColor;\n        this.textColor = textColor;\n    }\n\n    toJSON() {\n        return {\n            id: this.id,\n            name: this.name,\n            bgColor: this.bgColor,\n            textColor: this.textColor\n        };\n    }\n}\n"